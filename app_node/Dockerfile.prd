FROM node:17
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
ARG APP_CROSS_ORIGIN_ALLOW
ENV APP_CROSS_ORIGIN_ALLOW ${APP_CROSS_ORIGIN_ALLOW}
RUN echo ${APP_CROSS_ORIGIN_ALLOW}
CMD [ "npm", "run", "start" ]